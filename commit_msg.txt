Initial commit of shared platform abstraction library

This library provides cross-platform abstractions for:
- File system operations (find, path manipulation)
- Console I/O (raw mode, cursor control)
- Threading and synchronization
- String utilities (strtok, strncpy_s, etc.)
- Dynamic string builder with JSON support

Supports Windows (Win32 API) and Linux (POSIX).
